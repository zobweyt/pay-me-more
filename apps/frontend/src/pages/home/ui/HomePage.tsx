import { Stack, Title } from "@mantine/core";

export const HomePage: React.FC = () => {
  return (
    <Stack gap="xs">
      <Title order={1} size="h2">
        Главная
      </Title>
    </Stack>
  );
};
