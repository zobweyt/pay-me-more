import { Stack, Title } from "@mantine/core";

export const FavoritesPage: React.FC = () => {
  return (
    <Stack gap="xs">
      <Title order={1} size="h2">
        Избранное
      </Title>
    </Stack>
  );
};
