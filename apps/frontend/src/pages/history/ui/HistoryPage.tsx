import { Stack, Title } from "@mantine/core";

export const HistoryPage: React.FC = () => {
  return (
    <Stack gap="xs">
      <Title order={1} size="h2">
        История
      </Title>
    </Stack>
  );
};
